/**
 * 媒资管理模块：媒资列表占位页。
 */

import { PlaceholderPage } from '../../shared/components/PlaceholderPage';

/**
 * 媒资列表路由页（占位）。
 * @returns 占位 UI
 */
export function AssetListPage() {
  return <PlaceholderPage title="媒资管理" description="媒资列表与检索将在后续任务实现。" />;
}
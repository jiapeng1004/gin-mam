/**
 * AI 能力模块：AI 功能占位页。
 */

import { PlaceholderPage } from '../../shared/components/PlaceholderPage';

/**
 * AI 占位路由页。
 * @returns 占位 UI
 */
export function AiPage() {
  return <PlaceholderPage title="AI（占位）" description="智能编目、检索等能力预留入口。" />;
}
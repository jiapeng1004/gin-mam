/**
 * 编目模块：编目占位页。
 */

import { PlaceholderPage } from '../../shared/components/PlaceholderPage';

/**
 * 编目路由页（占位）。
 * @returns 占位 UI
 */
export function CatalogPage() {
  return <PlaceholderPage title="编目" />;
}
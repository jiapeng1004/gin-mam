/**
 * 系统管理模块：系统配置占位页。
 */

import { PlaceholderPage } from '../../shared/components/PlaceholderPage';

/**
 * 系统管理路由页（占位）。
 * @returns 占位 UI
 */
export function SystemPage() {
  return <PlaceholderPage title="系统管理" />;
}
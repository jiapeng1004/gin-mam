/**
 * 转码模块：转码任务占位页。
 */

import { PlaceholderPage } from '../../shared/components/PlaceholderPage';

/**
 * 转码路由页（占位）。
 * @returns 占位 UI
 */
export function TranscodePage() {
  return <PlaceholderPage title="转码" />;
}
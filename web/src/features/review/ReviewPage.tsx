/**
 * 审核中心模块：审核占位页。
 */

import { PlaceholderPage } from '../../shared/components/PlaceholderPage';

/**
 * 审核中心路由页（占位）。
 * @returns 占位 UI
 */
export function ReviewPage() {
  return <PlaceholderPage title="审核中心" />;
}